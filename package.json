{
  "name": "dynamic-scheduler-core",
  "version": "1.0.0",
  "description": "Pure-logic core for the Dynamic Group Scheduler (runs under Node and Apps Script V8).",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "node --test \"test/*.test.mjs\""
  }
}
